class Peer():
    def __self__(host, port):
        self.host = host
        self.port = port
        self.sharing = None

    def __repr__():
        return "[Peer {}:{}".format(self.host, self.port)
